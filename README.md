# Installation 
Before executing the code, install the backages in the requirements.txt file `pip install -r requirements.txt` in the terminal.

# Runing
Run the file nifti2dicom with `python nifti2dicom.py` in terminal.
